#Write a program to Count Iterations of a Loop Until a Condition
fn main() {
    let mut count = 0;
    let mut number = 1;

    while number < 100 {
        number *= 2;
        count += 1;
    }

    println!("Total iterations: {}", count);
}
